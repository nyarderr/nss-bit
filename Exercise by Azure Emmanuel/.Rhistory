# Calculate murder rate = total murders / population * 100,000
library(dslabs)
library(tidyverse)
#Question 1----
murder_rate <- murders$total / murders$population * 100000
murder_rate
low <- murder_rate < 1
low
#2----
indices <- which(low)
indices
murders$state[low]
#question 3----
# Get the names of states with murder rate < 1
low_states <- murders$state[indices]
low_states
# Question 4----
# Create logical vector for Northeast region
northeast <- murders$region == "Northeast"
# Combine conditions: low murder rate AND Northeast region
low_northeast <- low & northeast
# Get indices using which()
indices <- which(low_northeast)
# Get state names
northeast_low_states <- murders$state[indices]
print(northeast_low_states)
#Question 5----
# Calculate the national average murder rate
avg_rate <- mean(murder_rate)
# Create logical vector for states below average
below_avg <- murder_rate < avg_rate
num_below_avg <- sum(below_avg)
print(num_below_avg)
#Question 6----
# Step 1: Define target abbreviations
target_abbs <- c("AK", "MI", "IA")
# Step 2: Find their positions in the abbreviations vector
indices <- match(target_abbs, murders$abb)
# Step 3: Extract corresponding state names
states <- murders$state[indices]
print(states)
#Question 7----
# Define abbreviations to check
abbs_to_check <- c("MA", "ME", "MI", "MO", "MU")
# Use %in% to see which are actual state abbreviations
are_actual <- abbs_to_check %in% murders$abb
print(are_actual)
# Define abbreviations to check
abbs_to_check <- c("MA", "ME", "MI", "MO", "MU")
# Use %in% to see which are actual state abbreviations
are_actual <- abbs_to_check %in% murders$abb
print(are_actual)
#  Check which nicknames are real
check_these <- c("MA", "ME", "MI", "MO", "MU")
are_they_real <- check_these %in% murders$abb
# Find which one is NOT real (use the ! magic switch)
are_they_fake <- !are_they_real
print(are_they_fake)
# Step 1: Define target abbreviations
target_abbs <- c("AK", "MI", "IA")
# Step 2: Find their positions in the abbreviations vector
indices <- match(target_abbs, murders$abb)
# Step 3: Extract corresponding state names
states <- murders$state[indices]
print(states)
print(states)
